<#assign licenseFirst = "/*">
<#assign licensePrefix = " * ">
<#assign licenseLast = " */">
<#include "${project.licensePath}">

<#if package?? && package != "">
package ${package};

import com.airhacks.afterburner.views.FXMLView;

</#if>
/**
 *
 * @author ${user}
 */
public class ${name} extends FXMLView {
    
}
